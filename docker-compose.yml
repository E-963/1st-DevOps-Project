version: "3.8"

services:
 main:
  build: .
  container_name: todolist-depi-project
  volumes:
    - ./:main
  ports:
    - "8000:8000" 
    
  command: [ "gunicorn", "--bind", "0.0.0.0:8000", "wsgi" ]
